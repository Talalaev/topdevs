import { GetMediaTypePipe } from './get-media-type.pipe';

describe('GetMediaTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMediaTypePipe();
    expect(pipe).toBeTruthy();
  });
});
