import { GetMediaExpansionPipe } from './get-media-expansion.pipe';

describe('GetMediaExpansionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMediaExpansionPipe();
    expect(pipe).toBeTruthy();
  });
});
